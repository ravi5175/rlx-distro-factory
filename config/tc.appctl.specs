set +h
umask 022
RLX=/rlxos/roots
RLX_HOST=$(echo ${MACHTYPE} | sed "s/-[^-]*/-cross/")
RLX_ARCH=$(uname -m)
RLX_TGT=${RLX_ARCH}-linux-musl
LC_ALL=POSIX
PATH=${RLX}/cross-tools/bin:/bin
unset CFLAGS
export MAKEFLAGS=-j8
export RLX LC_ALL PATH RLX_ARCH RLX_HOST RLX_TGT
