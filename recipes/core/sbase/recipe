# Description: collection of unix tools that are portable across unix-systems
# URL: https://git.suckless.org/sbase/
# Maintainer: itsmanjeet@rlxos.dev
# Depends on: musl
# Category: system
# License: MIT

name=sbase
version=0.1.0
release=1
source="$name-$version.tar.gz::https://github.com/releax/sbase/archive/$version.tar.gz
		config.mk"

build() {
	cd $name-$version
	
	cp $src/config.mk .
	
	make install DESTDIR=$pkg
}
